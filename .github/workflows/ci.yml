name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  packages:
    name: Swift packages
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - name: Test
        working-directory: Packages/Scopa
        run: swift test

  server:
    name: Worker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
      - name: Test
        working-directory: Server
        run: npm test

  app:
    name: iOS app
    runs-on: macos-26  # the app uses iOS 26 APIs, so it needs Xcode 26
    steps:
      - uses: actions/checkout@v5
      # A generic destination needs no named simulator, so this does not break when the
      # runner image changes its device list. Signing is off: a fork has no certificates.
      - name: Build
        run: |
          xcodebuild build \
            -project Scopa.xcodeproj \
            -scheme Scopa \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            -quiet
